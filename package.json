{
  "name": "eslint-plugin-fta",
  "version": "0.0.2",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm run build && npm publish"
  },
  "keywords": [
    "fta",
    "eslint",
    "eslintplugin",
    "typescript",
    "complexity"
  ],
  "author": "Anthony Astige",
  "license": "ISC",
  "description": "ESLint plugin for FTA (Fast TypeScript Analyzer) complexity analysis. Enforces file-level complexity thresholds based on FTA's scoring system.",
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript-eslint/parser": "^8.26.1",
    "@typescript-eslint/rule-tester": "^8.26.1",
    "@typescript-eslint/utils": "^8.26.1",
    "eslint": "^9.22.0",
    "fta-cli": "^2.0.1",
    "typescript": "^5.8.2"
  },
  "peerDependencies": {
    "eslint": ">=9.0.0",
    "fta-cli": ">=2.0.0"
  }
}
